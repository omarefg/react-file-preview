import styled from 'styled-components'

export const UnsupportedStyles = styled.span`
    .unsupported {
        padding: 46px;
        background: white;
        margin: auto;
        text-align: center;
    }
`
